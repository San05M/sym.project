<!--Navigation Part -->
<div class="nav-main">
	<div class="container">
		<nav class="navbar navbar-expand-lg navbar-light">
			<a class="navbar-brand font-weight-bold" href="index.php">art.studio</a>
			<button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
				<span class="navbar-toggler-icon"></span>
			</button>
			<div class="collapse navbar-collapse" id="navbarNav">
				<ul class="navbar-nav mi auto">
					{% if app.request.pathinfo=='/index' or app.request.pathinfo=='/home' %}
						<li class="active lien">
					{% else %}
						<li class="lien">
					{% endif %}
						   <a href={{ path('sym_index') }}>
                           		<i> Home </i>
							</a>
						</li>
					{% if app.request.pathinfo=='/index' or app.request.pathinfo=='/galeria' %}
						<li class="active lien">
					{% else %}
						<li class="lien">
					{% endif %}
							<a href={{ path('sym_galeria') }}>
                           		<i> Galeria </i>
							</a>
						</li>
					{% if app.request.pathinfo=='/index' or app.request.pathinfo=='/proyceto' %}
						<li class="active lien">
					{% else %}
						<li class="lien">
					{% endif %}
							<a href={{ path('sym_perfil') }}>
                           		<i> Perfil </i>
							</a>
						</li>
				</ul>

				<ul class="navbar-nav ml-auto">
					{% if app.request.pathinfo=='/index' or app.request.pathinfo=='/login' %}
						<li class="active lien">
					{% else %}
						<li class="lien">
					{% endif %}
							<a href={{ path('sym_login') }}>
                           		<i> Ya soy miembro </i>
							</a>
						</li>
					{% if app.request.pathinfo=='/index' or app.request.pathinfo=='/register' %}
						<li class="active lien">						
                    {% else %}
						<li class="lien">
					{% endif %}
							<a href={{ path('sym_index') }}>
                           		<i> ¡Únete a nosotros! </i>
							</a>
						</li>
				</ul>
			</div>
		</nav>
<!--end:Nav -->
	</div>
</div>
<!--end:Navigation -->
